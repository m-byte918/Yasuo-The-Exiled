using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameEnd : MonoBehaviour
{
    GameObject boss;

    // Start is called before the first frame update
    void Start()
    {
        boss = this.GetComponent<GameObject>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
